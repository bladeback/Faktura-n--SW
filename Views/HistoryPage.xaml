<UserControl x:Class="InvoiceApp.Views.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InvoiceApp.ViewModels"
             xmlns:models="clr-namespace:InvoiceApp.Models"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">

    <UserControl.DataContext>
        <vm:HistoryViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Historie dokladů" Style="{StaticResource Heading1}" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Documents}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  RowStyle="{StaticResource ModernRowStyle}"
                  CellStyle="{StaticResource ModernCellStyle}"
                  ColumnHeaderStyle="{StaticResource ModernHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Číslo" Binding="{Binding Number}" Width="120"/>
                <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Datum" Binding="{Binding IssueDate, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Odběratel" Binding="{Binding Customer.Name}" Width="*"/>
                
                <DataGridTemplateColumn Header="Zaplaceno" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsPaid, Mode=OneWay}" 
                                      Command="{Binding DataContext.MarkAsPaidCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      CommandParameter="{Binding}"
                                      HorizontalAlignment="Center">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox" BasedOn="{StaticResource FormCheckBox}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="{x:Static models:DocType.Order}">
                                                <Setter Property="Visibility" Value="Hidden"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Upravit" 
                                    Command="{Binding DataContext.EditDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource ToolbarButton}"
                                    Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
