<UserControl x:Class="InvoiceApp.Views.CustomersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{DynamicResource Brush.AppBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Horní pruh: název vlevo, toolbar vpravo -->
        <Grid Grid.Row="0" Margin="16,12,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Titulek -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Odběratelé" Style="{StaticResource Heading1}"/>
                <TextBlock Text="{Binding Count, StringFormat='  ({0})'}"
                           Margin="6,0,0,0"
                           VerticalAlignment="Center"
                           Style="{StaticResource SubtleText}"/>
            </StackPanel>

            <!-- Toolbar -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Přidat" Click="Add_Click" Style="{StaticResource ToolbarButton}"/>
                <Button Content="Smazat vybrané" Click="DeleteSelected_Click" Style="{StaticResource ToolbarButton}"/>


                <!-- mezera -->
                <Border Width="12" Background="Transparent"/>

                <!-- Hledání -->
                <TextBox x:Name="SearchBox"
                         Style="{StaticResource SearchBox}"
                         Width="300"
                         VerticalAlignment="Center"
                         ToolTip="Hledat (název, IČO, město, e-mail)…"
                         TextChanged="SearchBox_TextChanged"/>
            </StackPanel>
        </Grid>

        <!-- CONTENT -->
        <DataGrid x:Name="CustomersGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding Customers, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Style="{StaticResource ModernDataGrid}"
                  Margin="16,0,16,16">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource ModernRowStyle}">
                    <Setter Property="Cursor" Value="Hand"/>
                    <EventSetter Event="PreviewMouseLeftButtonUp" Handler="Row_Click"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Název" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="IČO" Binding="{Binding ICO}" Width="100"/>
                <DataGridTextColumn Header="Město" Binding="{Binding City}" Width="150"/>
                <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
