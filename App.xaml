<Application x:Class="InvoiceApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:InvoiceApp"
             xmlns:vm="clr-namespace:InvoiceApp.ViewModels"
             xmlns:models="clr-namespace:InvoiceApp.Models"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             xmlns:conv="clr-namespace:InvoiceApp.Converters">

  <Application.Resources>
    <ResourceDictionary>

      <!-- Globální styly UI -->
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Styles/Theme.xaml"/>
      </ResourceDictionary.MergedDictionaries>

      <!-- TVŮJ původní provider pro DocType -->
      <ObjectDataProvider x:Key="DocTypes"
                          MethodName="GetValues"
                          ObjectType="{x:Type sys:Enum}">
        <ObjectDataProvider.MethodParameters>
          <x:Type TypeName="models:DocType"/>
        </ObjectDataProvider.MethodParameters>
      </ObjectDataProvider>

      <!-- Globální konvertory -->
      <conv:DecimalCommaConverter x:Key="DecimalComma"/>
      <conv:WholeNumberConverter  x:Key="WholeNumber"/>
      <conv:VatPercentConverter   x:Key="VatPercent"/>
      <conv:IbanDisplayConverter  x:Key="IbanDisplay"/>

    </ResourceDictionary>
  </Application.Resources>
</Application>
